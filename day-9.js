/*
Create a new array called allItems that contains all items in a single array.
*/

const shoppingList = [['apple', 'banana'], ['milk', 'cheese'], ['bread']];

const allItems = shoppingList.flat(3);

console.log(allItems);
